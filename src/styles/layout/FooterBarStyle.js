import styled from 'styled-components';

const Colors = {

  TOP_BAR_BACKGROUND_COLOR: '#CF5808',
}

const FooterBarStyle = styled.div`
  .footerBar {
    background: ${Colors.TOP_BAR_BACKGROUND_COLOR};
    padding: 8px 15px;
  }
}
`;

export default FooterBarStyle;
