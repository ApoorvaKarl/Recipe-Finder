import styled from 'styled-components';

const Colors = {
  PRIMARY_COLOR: '#D95B1C'
}

const CustomSubmitButtonStyle = styled.div`
  .ant-btn {
    background: ${Colors.PRIMARY_COLOR};
  }
`;

export default CustomSubmitButtonStyle;
